# PyTorch ist im Base-Image enthalten, daher hier nicht nötig
# torch==2.8.0+cu128

# neueste Diffusers direkt von GitHub, falls Features benötigt werden
git+https://github.com/huggingface/diffusers.git

transformers
accelerate
RealESRGAN
xformers
invisible_watermark
safetensors
boto3
flask
gunicorn

# Qwen-Image (DFloat11) Dependencies
dfloat11[cuda12]

# FastAPI + ASGI Server (Production-ready)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9  # For file uploads
pydantic>=2.9.0
